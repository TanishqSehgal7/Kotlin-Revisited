package ClassesInKotlin

object ImportantConstants {

/*    Everytime we create an object like this, it means that we
      are making a singleton. A singleton/object is an entity that
      just has a single instance of itself, ie we cannot define
      any instances rather use it directly with the object name
      itself
 */
    val PI = 3.141592

    fun myFunc() {
        println("This is a demo function from singleton")
    }

}